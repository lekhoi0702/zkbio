@page "/contractor-access-history"
@model ZkbioDashboard.Pages.ContractorAttendanceModel
@{
    ViewData["Title"] = "Contractor Access History";
}

@section Scripts {
    <script>
        function viewDetails(pin, date, name) {
            ZKBioUi.openTransactionDetails({
                employeeHeader: name + ' (' + pin + ')',
                dateHeader: date,
                url: '?handler=Details',
                requestData: { pin: pin, date: date },
                noRecordsText: 'No records found.',
                errorText: 'Error fetching details.'
            });
        }
    </script>
}

<section class="content report-shell" style="padding:0; --at-filter-col-min: 140px;">
    <div class="container-fluid px-0 d-flex flex-column" style="height:calc(100vh - 57px); overflow:hidden; gap:0;">

        <div class="at-page-header">
            <h1>Contractor Access History</h1>
        </div>

        <div class="at-filter-card">
            <div class="at-filter-body">
                <form method="get">
                    <div class="at-filter-grid">
                        <div>
                            <label>From</label>
                            <input type="datetime-local" asp-for="FromDate" class="form-control" step="1" />
                        </div>
                        <div>
                            <label>To</label>
                            <input type="datetime-local" asp-for="ToDate" class="form-control" step="1" />
                        </div>
                        <div>
                            <label>Factory</label>
                            <select asp-for="Factory" class="form-control">
                                <option value="">-- All --</option>
                                @foreach (var factory in Model.Factories)
                                {
                                    <option value="@factory">@factory</option>
                                }
                            </select>
                        </div>
                        <div>
                            <label>PIN</label>
                            <input type="text" asp-for="Pin" class="form-control" />
                        </div>
                        <div class="at-filter-actions">
                            <button type="submit" class="at-btn at-btn-primary" title="Apply">
                                <i class="fas fa-search"></i>
                            </button>
                            <a asp-page="./ContractorAttendance" class="at-btn at-btn-ghost" title="Reset">
                                <i class="fas fa-undo-alt"></i>
                            </a>
                        </div>
                    </div>
                </form>
            </div>
        </div>

        <div class="at-table-card">
            <div class="at-table-scroll">
                <table class="at-table js-sortable-table">
                    <thead>
                        <tr>
                            <th>Factory</th>
                            <th>Dept</th>
                            <th>PIN</th>
                            <th>Name</th>
                            <th>Shift Date</th>
                            <th>Gate In</th>
                            <th>Gate Out</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody id="skeleton-loader">
                        @for (int i = 0; i < 18; i++)
                        {
                            <tr>
                                @for (int j = 0; j < 8; j++) { <td><div class="skeleton skeleton-text"></div></td> }
                            </tr>
                        }
                    </tbody>
                    <tbody id="table-data">
                        @if (Model.Records != null && Model.Records.Any())
                        {
                            foreach (var item in Model.Records)
                            {
                                <tr>
                                    <td>@item.Factory</td>
                                    <td>@item.DeptName</td>
                                    <td>@item.Pin</td>
                                    <td>@item.FullName</td>
                                    <td>@item.Date.ToString("yyyy-MM-dd")</td>
                                    <td>@(item.GateIn?.ToString("HH:mm:ss") ?? "-")</td>
                                    <td>@(item.GateOut?.ToString("HH:mm:ss") ?? "-")</td>
                                    <td>
                                        <button type="button" class="at-btn-detail"
                                                onclick="viewDetails('@item.Pin', '@item.Date.ToString("yyyy-MM-dd")', '@item.FullName')">
                                            Details
                                        </button>
                                    </td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr><td colspan="8" class="at-empty">No contractor records found for the selected criteria.</td></tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>

    </div>
</section>

@await Html.PartialAsync("_TransactionDetailsModal")

